{
  "name": "@demo/app-order",
  "version": "0.0.0",
  "private": true,
  "description": "订单子应用：可独立开发、独立打包、独立部署，也可被基座作为微应用加载",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5174 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 4174 --strictPort",
    "typecheck": "vue-tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@demo/shared-utils": "workspace:*",
    "@demo/ui-package": "workspace:*",
    "vue": "catalog:",
    "vue-router": "catalog:"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "catalog:",
    "vite": "catalog:",
    "vue-tsc": "catalog:"
  }
}
